#name=T.pradeep
#lab=4
#task=1
# 1. ASSIGNMENT STATEMENT
# Storing a list of numbers in a variable
numbers = [1, 2, 3, 4, 5, 6]

# 2. LOOP STATEMENT
# Iterating through each number in the list
for number in numbers:

# 3. CONDITIONAL IF STATEMENT
    # Checking if the current number is even
    if number % 2 == 0:

# 4. PRINT STATEMENT
        # Displaying the even number to the screen
        print(f"{number} is an even number.")